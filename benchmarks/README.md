# Local hardware measurements

Benchmark JSON, sample streams and process-resource logs are generated locally
and ignored by Git. They are retained as engineering evidence during cleanup.
Do not infer device support or performance from software-only fixtures.

See `scripts/run-usb-stress.ps1` and `docs/VALIDATION.md` for the hardware workflow.
